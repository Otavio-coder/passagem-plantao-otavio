# Adicione este bloco dentro do server {} do hmartinica04 no nginx
# (mesmo server block que já serve o app em HTTPS)
#
# Depois: sudo nginx -t && sudo systemctl reload nginx

location /soketi/ {
    proxy_pass             http://127.0.0.1:6001/;
    proxy_http_version     1.1;
    proxy_set_header       Upgrade $http_upgrade;
    proxy_set_header       Connection "Upgrade";
    proxy_set_header       Host $host;
    proxy_set_header       X-Real-IP $remote_addr;
    proxy_set_header       X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header       X-Forwarded-Proto $scheme;
    proxy_read_timeout     86400s;
    proxy_send_timeout     86400s;
}

# Alternativa: servidor separado na porta 6001 com SSL próprio
# (use esta opção se o app já usa a porta 443 sem possibilidade de adicionar location)
#
# server {
#     listen 6001 ssl;
#     server_name hmartinica04.santacasa.org.br;
#
#     ssl_certificate     /etc/ssl/certs/hmartinica04.crt;
#     ssl_certificate_key /etc/ssl/private/hmartinica04.key;
#
#     location / {
#         proxy_pass             http://127.0.0.1:6001;
#         proxy_http_version     1.1;
#         proxy_set_header       Upgrade $http_upgrade;
#         proxy_set_header       Connection "Upgrade";
#         proxy_set_header       Host $host;
#         proxy_read_timeout     86400s;
#     }
# }
